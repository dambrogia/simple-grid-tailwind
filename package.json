{
  "name": "simple-grid",
  "version": "1.0.0",
  "description": "Create a lightweight grid on the fly. Good for a small-scoped drop-in grid when needed.",
  "scripts": {
    "build": "postcss ./src/style.scss -o dist/compiled.css",
    "debug": "node --inspect-brk=9229 --nolazy ./node_modules/.bin/postcss ./src/style.scss -o dist/compiled.css"
  },
  "author": "Dominic Dambrogia <domdambrogia@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@csstools/postcss-sass": "^4.0.0",
    "autoprefixer": "^9.6.1",
    "cssnano": "^4.1.10",
    "postcss": "^7.0.17",
    "postcss-cli": "^6.1.3",
    "tailwindcss": "^1.1.2"
  },
  "dependencies": {
    "minimist": "^1.2.0"
  }
}
