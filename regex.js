/**
 * Adjust this file to your needs. Declare which patterns of selectors
 * should be kept. All others will be deleted.
 */
module.exports = [
    // Rows
    /flex-row$/,
    /flex-row */,
    // Columns
    /flex-[0-9]/,
    // Gutters
    /-mx-[0-9]/,
    /px-[0-9]/,
    // Containers
    /max-w-/,
    /w-/,
];
